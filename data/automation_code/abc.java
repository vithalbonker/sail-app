import static com.jayway.restassured.RestAssured.given;
import org.junit.Test;

public class abc {

	@Test
	public void testMethod() {
		given().when().get("abc1");
given().when().get("abc2");
given().when().get("abc3");

	}
}